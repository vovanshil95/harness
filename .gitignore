# Секреты — реальные значения только локально и на сервере
.env

# Данные агента: сессии, авторизация, кеши, память.
# Содержит auth.json и токены — в репозиторий не попадает никогда.
hermes-data/

# Прочее
*.bak
*.log
.DS_Store
